<div>
{% for categorie in article.getcategories %}
    <a href="{{ path('categorie_show', {'id': categorie.id}) }}"><span class="badge badge-warning">{{ categorie.nom }}</span></a>
{% endfor %}
</div>